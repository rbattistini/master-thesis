\usepackage{listings}
\usepackage{xcolor}

%-----------------------------------------------------
% LISTING CONFIGURATIONS
%-----------------------------------------------------

\crefname{lstlisting}{listing}{listing}	
\Crefname{lstlisting}{Listing}{Listing}
\renewcommand\lstlistlistingname{List of Listings}

\lstset{
    showstringspaces=false,
    basicstyle=\footnotesize\ttfamily,
    keywordstyle=\color{blue}\bfseries,
    ndkeywordstyle=\color{teal},
    identifierstyle=\color{black},
    stringstyle=\color{purple},
    commentstyle=\color{gray}\itshape,
    emphstyle=\color{violet},
    breaklines=true,
    breakatwhitespace=true,
    tabsize=2,
    frame=single,
    frameround=tttt,
    numbers=left,
    numberstyle=\tiny\color{gray},
    numbersep=10pt,
    escapeinside={(*@}{@*)},
    columns=fullflexible,
    extendedchars=true,
    literate={∞}{{$\infty$}}1 {→}{{$\rightarrow$}}1,
}


\lstdefinelanguage{Kotlin}{
    morekeywords={
        abstract, actual, annotation, as, as?, break, by, catch, class, companion, const,
        constructor, continue, crossinline, delegate, do, dynamic, else, enum, expect, external,
        false, field, final, finally, for, fun, get, if, import, in, infix, init, inline, inner,
        interface, internal, is, it, lateinit, noinline, null, object, open, operator, out,
        override, package, param, private, property, protected, public, reified, return, sealed,
        set, super, suspend, tailrec, this, throw, true, try, typealias, typeof, val, var,
        vararg, when, where, while, yield
    },
    morecomment=[l]{//},
    morecomment=[s]{/*}{*/},
    morestring=[b]",
    morestring=[s]{"""}{"""},
    sensitive=true,
    emph={
        println, print, map, flatMap, filter, fold, forEach, takeIf, let, also, apply, run,
        with, use, lazy, toList, toSet, toMap, firstOrNull, sequence, yield, Array, List, Set,
        Map, String, Int, Long, Float, Double, Boolean, Char, Byte, Short
    },
    ndkeywords={
        @Deprecated, @JvmField, @JvmName, @JvmOverloads, @JvmStatic, @JvmSynthetic,
        @Inject, @Provides, @Module, @Singleton, @Component, @Binds, @OptIn
    }
}
